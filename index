<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Доступ к документу</title>
  <style>
    :root{--border:#e5e7eb;--muted:#6b7280;--okbg:#ecfdf5;--ok:#065f46;--errbg:#fef2f2;--err:#991b1b;--primary:#2563eb}
    *{box-sizing:border-box}
    body{margin:0;padding:24px;font-family:system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;background:#fafafa}
    .wrap{max-width:880px;margin:0 auto}
    .card{background:#fff;border:1px solid var(--border);border-radius:16px;padding:24px;box-shadow:0 1px 2px rgba(0,0,0,.04)}
    h1{margin:0 0 8px;font-size:28px;line-height:1.2}
    .muted{color:#6b7280;margin-bottom:16px}
    .status{margin-top:12px;padding:12px;border-radius:10px;display:none}
    .status.ok{display:block;background:#ecfdf5;color:#065f46}
    .status.err{display:block;background:#fef2f2;color:#991b1b}
    .row{margin-top:16px}
    label{display:block;font-size:14px;margin-bottom:6px;color:#374151}
    input[type=email]{padding:12px;border:1px solid #d1d5db;border-radius:10px;width:100%;font-size:16px}
    .buttons{display:flex;gap:10px;align-items:center;margin-top:16px}
    .btn{padding:10px 16px;border:0;border-radius:10px;cursor:pointer;font-weight:600}
    .btn[disabled]{opacity:.6;cursor:not-allowed}
    .btn-primary{background:#2563eb;color:#fff}
    .btn-ghost{background:#f3f4f6}
    .small{font-size:12px;color:#6b7280}
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <h1>Доступ к документу</h1>
      <div class="muted">Страница общая. У каждого — персональный <code>token</code> в ссылке.</div>

      <div id="status" class="status"></div>

      <div class="row">
        <label for="email">Ваш Google-email</label>
        <input id="email" type="email" placeholder="name@gmail.com" autocomplete="email" />
      </div>

      <div class="buttons">
        <button id="claimBtn" class="btn btn-primary">Получить доступ</button>
        <a id="openBtn" class="btn btn-ghost" style="display:none" target="_blank" rel="noopener">Открыть документ</a>
      </div>

      <div class="row small">Если кнопка неактивна — вы открыли страницу без параметра <code>?token=...</code>.</div>
    </div>
  </div>

  <script defer src="app.js"></script>
</body>
</html>
